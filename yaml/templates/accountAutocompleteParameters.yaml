- name: filter
  in: query
  description: Filter the autocomplete object for accounts.
  required: false
  schema:
    $ref: '#/components/schemas/AutocompleteFilter'
  style: deepObject
  explode: true
