RecurrenceRead:
  type: object
  properties:
    type:
      type: string
      format: string
      example: "recurrences"
      description: "Immutable value"
    id:
      type: integer
      format: int32
      example: 2
    attributes:
      $ref: '#/components/schemas/Recurrence'
    links:
      $ref: '#/components/schemas/ObjectLink'
