AutocompleteTransactionID:
  type: object
  required:
    - id
    - name
    - description
  properties:
    id:
      type: integer
      format: int32
      example: 2
      readOnly: true
    name:
      type: string
      format: string
      example: "#12: Transaction"
      description: Transaction description with ID
      readOnly: true
    description:
      type: string
      format: string
      example: "#12: Transaction"
      description: Transaction description with ID
      readOnly: true