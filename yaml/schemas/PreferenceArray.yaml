PreferenceArray:
  type: object
  properties:
    data:
      type: array
      items:
        $ref: '#/components/schemas/Preference'
    meta:
      $ref: '#/components/schemas/Meta'
    links:
      $ref: '#/components/schemas/PageLink'