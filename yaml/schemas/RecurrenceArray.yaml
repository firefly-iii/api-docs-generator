RecurrenceArray:
  type: object
  properties:
    data:
      type: array
      items:
        $ref: '#/components/schemas/Recurrence'
    meta:
      $ref: '#/components/schemas/Meta'
    links:
      $ref: '#/components/schemas/PageLink'