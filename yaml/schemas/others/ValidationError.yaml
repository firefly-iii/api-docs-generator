ValidationError:
  type: object
  properties:
    message:
      type: string
      format: string
      example: "The given data was invalid."
    errors:
      type: object
      properties:
        field1:
          type: array
          items:
            type: string
            format: string
        field2:
          type: array
          items:
            type: string
            format: string
