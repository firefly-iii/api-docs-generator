RuleGroupUpdate:
  type: object
  required:
    - title
  properties:
    title:
      type: string
      format: string
      example: "New rule group title"
    active:
      type: boolean
      format: boolean
      example: true
    description:
      type: string
      format: string
      example: "New rule group description"