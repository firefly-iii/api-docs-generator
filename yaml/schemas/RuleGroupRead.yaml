RuleGroupRead:
  type: object
  properties:
    type:
      type: string
      format: string
      example: "rules_group"
      description: "Immutable value"
    id:
      type: integer
      format: int32
      example: 2
    attributes:
      $ref: '#/components/schemas/RuleGroup'
    links:
      $ref: '#/components/schemas/ObjectLink'
