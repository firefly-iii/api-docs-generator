PreferenceRead:
  type: object
  required:
    - type
    - id
    - attributes
  properties:
    type:
      type: string
      format: string
      example: "preferences"
      description: "Immutable value"
    id:
      type: integer
      format: int32
      example: 2
    attributes:
      $ref: '#/components/schemas/Preference'
