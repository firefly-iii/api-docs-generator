UserRead:
  type: object
  properties:
    type:
      type: string
      format: string
      example: "users"
      description: "Immutable value"
    id:
      type: integer
      format: int32
      example: 2
    attributes:
      $ref: '#/components/schemas/User'
    links:
      $ref: '#/components/schemas/ObjectLink'
