RuleRead:
  type: object
  required:
    - type
    - id
    - attributes
    - links
  properties:
    type:
      type: string
      format: string
      example: "rules"
      description: "Immutable value"
    id:
      type: integer
      format: int32
      example: 2
    attributes:
      $ref: '#/components/schemas/Rule'
    links:
      $ref: '#/components/schemas/ObjectLink'
