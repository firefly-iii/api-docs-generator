RuleRead:
  type: object
  properties:
    type:
      type: string
      format: string
      example: "rules"
      description: "Immutable value"
    id:
      type: string
      format: string
      example: 2
    attributes:
      $ref: '#/components/schemas/Rule'
    links:
      $ref: '#/components/schemas/ObjectLink'
