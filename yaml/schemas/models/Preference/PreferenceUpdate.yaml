PreferenceUpdate:
  type: object
  required:
    - data
  properties:
    data:
      oneOf:
        - type: boolean
        - type: string
        - type: object
        - type: array
          items:
            type: string
            format: string
      example: "EUR"
      description: "The actual preference content."
