BudgetRead:
  type: object
  properties:
    type:
      type: string
      format: string
      example: "budgets"
      description: "Immutable value"
    id:
      type: string
      format: string
      example: 2
    attributes:
      $ref: '#/components/schemas/Budget'
