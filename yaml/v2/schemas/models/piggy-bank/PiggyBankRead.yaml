PiggyBankV2Read:
  type: object
  required:
    - type
    - id
    - attributes
    - links
  properties:
    type:
      type: string
      format: string
      example: "piggy_banks"
      description: "Immutable value"
    id:
      type: string
      format: string
      example: "2"
    attributes:
      $ref: '#/components/schemas/PiggyBankV2'
    links:
      $ref: '#/components/schemas/ObjectLinkV2'
